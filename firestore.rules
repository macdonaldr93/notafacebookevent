service cloud.firestore {
  match /databases/{database}/documents {
    match /events/{event}/timeline/{timelineId} {
      allow create;
      allow read;
    }

    match /events/{event}/guests/{guest} {
      allow create;
      allow read;
    }

    match /events/{event}/admin {
      allow create;
    }

    match /events/{event} {
      allow update, delete: if request.resource.data.admin.managePassword == resource.data.admin.managePassword;
      allow create;
      allow read;
    }

    match /{document=**} {
      allow read;
    }
  }
}
